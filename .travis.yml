language: node_js
node_js:
- "stable"
cache:
  directories:
  - node_modules
script:
- npm test
- npm run build
deploy:
  provider: surge
  domain: studio.votoinformado2019.com
  project: ./build/
  on:
    branch: master
